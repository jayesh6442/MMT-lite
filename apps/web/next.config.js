/**
 * @type {import('next').NextConfig}
 */
module.exports = {
  // Allow dev requests from any origin (useful for mobile/other devices on LAN).
  allowedDevOrigins: ['*'],
  reactStrictMode: true,
};
